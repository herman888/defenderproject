{
  "name": "projectlarp",
  "private": true,
  "description": "Project LARP documentation site (MkDocs Material)",
  "scripts": {
    "build": "python3 -m pip install -r requirements-docs.txt && python3 -m mkdocs build -d public",
    "dev": "mkdocs serve -a 127.0.0.1:8000"
  }
}
