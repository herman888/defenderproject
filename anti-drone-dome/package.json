{
  "name": "projectlarp",
  "private": true,
  "description": "Project LARP documentation (static public/ for Vercel)",
  "scripts": {
    "build": "echo prebuilt",
    "docs:build": "mkdocs build -d public",
    "docs:dev": "mkdocs serve -a 127.0.0.1:8000"
  }
}
