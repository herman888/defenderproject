# Windows bench-day dependencies.  Keep this separate from the simulator-only
# requirements so OpenCV GUI capture is never replaced by a headless wheel.
opencv-python==4.11.0.86
psutil==7.0.0
pyserial==3.5
ultralytics==8.3.151
