# Harmless file so a leftover Vercel "pip3 install -r requirements-docs.txt"
# override does not fail when Root Directory is the repository root.
# Real docs deps live in anti-drone-dome/requirements-docs.txt.
